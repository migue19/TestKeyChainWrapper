# Uncomment the next line to define a global platform for your project
platform :ios, '12.1'

source 'https://github.com/CocoaPods/Specs.git'
source 'https://github.com/migue19/KeychainWrapperPodSpecs.git'

target 'TestKeyChainWrapper' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'KeychainWrapper', '~> 0.0.2'

  # Pods for TestKeyChainWrapper

  target 'TestKeyChainWrapperTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestKeyChainWrapperUITests' do
    # Pods for testing
  end

end
